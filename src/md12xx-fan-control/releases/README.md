# Development release

`md12xx.fancontrol.plg` is the current standalone beta installer. Verify it against `SHA256SUMS` before testing.

The controller installs disabled. Configure and commission each shelf before enabling it.

