# Integrated Health component

This directory contains the Health banner component source consumed by the sibling `waz-dashboard-plugin` build.

The v0.8.0 build transforms the component paths at package time and installs it under the single `/usr/local/emhttp/plugins/waz.dashboard` runtime. It is retained as a separate source component for development clarity; no standalone WAZ Health release is published here.
